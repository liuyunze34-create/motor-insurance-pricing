# Generated figures

`run_project.py` recreates the project charts in this folder, including claim-count distribution, observed frequency by rating factor, risk-decile calibration, ordered Lorenz curve, pricing relativities and repeated-validation stability.

The compact public repository keeps the charts reproducible from code instead of storing duplicate generated image files.
