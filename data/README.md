# Data

The raw `freMTPL2freq` and `freMTPL2sev` CSV files are intentionally not committed to this public repository.

Run:

```bash
python download_data.py
```

to download the OpenML source tables into this folder. Keeping the raw files out of version control makes the portfolio repository smaller while preserving reproducibility.
