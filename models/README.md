# Generated model files

`run_project.py` writes the fitted preprocessing objects and pricing models to this folder. The binary `.joblib` files are generated locally and are not committed to the public repository.

Run:

```bash
python download_data.py
python run_project.py
```

before using the live policy example in `app.py`.
